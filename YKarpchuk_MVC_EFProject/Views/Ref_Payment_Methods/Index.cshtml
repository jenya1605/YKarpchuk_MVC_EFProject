@model IEnumerable<YKarpchuk_MVC_EFProject.Models.Ref_Payment_Methods>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Payment_Method_Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Payment_Method_Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Payment_Method_Code }) |
            @Html.ActionLink("Details", "Details", new { id=item.Payment_Method_Code }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Payment_Method_Code })
        </td>
    </tr>
}

</table>
