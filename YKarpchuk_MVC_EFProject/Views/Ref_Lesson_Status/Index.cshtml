@model IEnumerable<YKarpchuk_MVC_EFProject.Models.Ref_Lesson_Status>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Lesson_Status_Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Lesson_Status_Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Lesson_Status_Code }) |
            @Html.ActionLink("Details", "Details", new { id=item.Lesson_Status_Code }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Lesson_Status_Code })
        </td>
    </tr>
}

</table>
